[project]
name = "station-dispatch-replay"
version = "1.0.0"
description = "共享储能站调度追责回放后端：不可变版本、WORM 封存、SQLite 哈希链"
requires-python = ">=3.11"

[project.scripts]
station-replay = "station_replay.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
