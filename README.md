# 储能电站调度回放

本仓库保存电站调度事件的协议约定和事故样例。现场消息都带有 `event_id`、设备侧序号与发生时间；接收时间仅用于审计，不能替代事件发生时间参与调度判断。

`reference/events.json` 给出一段含重复指令和迟到遥测的值班记录。功率正值表示放电，负值表示充电，荷电状态使用百分数。后续服务应以这些字段作为外部系统交换边界，并保留未知扩展字段以便协议升级。

项目采用 Python 3.11 及以上版本。执行 `python -m unittest discover -s tests -v` 可检查随仓库提供的资料是否完整。
